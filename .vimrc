" Install of pathogen
execute pathogen#infect()

" turn on color syntax
syntax on

" make line numbering the default
set number

" Allow hidden buffers
set hidden

" Allow plugins
filetype plugin indent on

" size of hard tabstop
set tabstop=4

" size of an indent
set shiftwidth=4

" always use spaces instead of tab character
set expandtab 

